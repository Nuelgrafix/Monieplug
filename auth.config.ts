// Authentication configuration - NextAuth removed
// This file is kept for compatibility but exports empty config

// Edge-safe config: minimal configuration for compatibility
export const authConfig = {
  providers: [],
  callbacks: {},
  pages: {
    signIn: "/signin",
  },
};
